# fn-15-07t.6 Update CLAUDE.md with Ecosystem documentation and cross-project references

## Description

Update the project's CLAUDE.md to document the new Ecosystem tab group, cross-project references, and the GitHub API integration. This ensures future Claude sessions understand the ecosystem context.

### Implementation

**File:** `CLAUDE.md`

**Additions:**

1. **Ecosystem section** — Add after the Interactive Proof System section:
   - Description of the 3 Ecosystem tabs
   - Cross-project GitHub URLs
   - Shared backend context
   - Test user reference table (consolidated)

2. **File Navigation updates** — Add new files to the navigation section:
   - `components/ecosystem/ecosystem-hub-tab.tsx`
   - `components/ecosystem/record-census-tab.tsx`
   - `components/ecosystem/test-users-tab.tsx`
   - `app/api/ecosystem/github-status/route.ts`
   - `app/api/v1/record-counts/route.ts` (already exists but not in CLAUDE.md)

3. **API Routes updates** — Document the new ecosystem routes

4. **Tab count update** — Update "23 tabs" references to "26 tabs"

## Acceptance

- [ ] Ecosystem section added to CLAUDE.md
- [ ] All 3 new component files listed in file navigation
- [ ] New API routes documented
- [ ] Tab count references updated (23 -> 26)
- [ ] Cross-project GitHub URLs included
- [ ] `pnpm build` still passes (CLAUDE.md changes shouldn't affect build)

## Done summary

TBD

## Evidence

- Commits:
- Tests:
- PRs:
