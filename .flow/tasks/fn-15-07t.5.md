# fn-15-07t.5 Wire 3 Ecosystem tabs into page.tsx navigation

## Description

Add the 3 new Ecosystem tabs to the main `app/page.tsx` navigation. This involves updating the ViewMode type union, adding viewModes entries, adding imports, and adding content rendering cases.

### Implementation

**File:** `app/page.tsx`

**Changes needed:**

1. **ViewMode type union** (currently at ~line 59): Add 3 new values:

   ```typescript
   | 'ecosystem-hub'
   | 'record-census'
   | 'test-users'
   ```

2. **Icon imports**: Add `Building2, BarChart3` from `lucide-react` (Users should already be imported).

3. **viewModes array** (currently at ~line 87): Add new group after the Proof System tabs:

   ```typescript
   { id: 'ecosystem-hub', label: 'Ecosystem', icon: Building2 },
   { id: 'record-census', label: 'Record Census', icon: BarChart3 },
   { id: 'test-users', label: 'Test Users', icon: Users },
   ```

4. **Component imports**: Add imports for the 3 new components:

   ```typescript
   import { EcosystemHubTab } from '@/components/ecosystem/ecosystem-hub-tab'
   import { RecordCensusTab } from '@/components/ecosystem/record-census-tab'
   import { TestUsersTab } from '@/components/ecosystem/test-users-tab'
   ```

5. **Content rendering** (currently ~line 150): Add cases:
   ```typescript
   {currentView === 'ecosystem-hub' && <EcosystemHubTab />}
   {currentView === 'record-census' && <RecordCensusTab />}
   {currentView === 'test-users' && <TestUsersTab />}
   ```

### Verification

After wiring, the tab navigation should show 26 total tabs (23 existing + 3 new), with the Ecosystem group appearing after the Interactive Proof System group.

## Acceptance

- [ ] 3 new tabs visible in navigation sidebar
- [ ] Tabs render correct components when clicked
- [ ] Icons display correctly (Building2, BarChart3, Users)
- [ ] Tab group visually separated from other groups
- [ ] No regressions in existing 23 tabs
- [ ] `pnpm build` passes
- [ ] `pnpm typecheck` passes

## Done summary

TBD

## Evidence

- Commits:
- Tests:
- PRs:
